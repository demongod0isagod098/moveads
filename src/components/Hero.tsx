import React from 'react';

const Hero: React.FC = () => {
    return (
        <div className="hero">
            <h1>Turn the City Into Your Moving Billboard</h1>
            <div className="cta-buttons">
                <button>Get Started</button>
                <button>Learn More</button>
            </div>
        </div>
    );
};

export default Hero;